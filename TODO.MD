## What to do
-[x] Load pdf into memory
-[x] Add handling errors for pdf not found
-[x] Clone pdf and delete the unnecessary pages
-[x] Save pdf to the same dir
-[x] Make it so you can call it from anywhere
-[x] Rewrite so the path is passed through command line arguments

## Error handling
-[x] Check if start/end_range is not greater than number of pages
-[x] Check if file exists
-[x] Better output naming so it will not overwrite the previous one
